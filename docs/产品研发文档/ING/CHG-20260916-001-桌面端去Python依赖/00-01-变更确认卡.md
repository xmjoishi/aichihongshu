# 桌面端去 Python 依赖：变更确认卡

任务：CHG-20260916-001

状态：实施中（阶段 A 已完成，P0 Rust SQLite vertical slice 已实现）。用户已明确“执行以上”，该授权覆盖本确认卡中已明确方向；未决事项仍只约束其对应的产品行为、数据语义和权限边界。

## 1. 用户已明确的方向

1. **最高优先级：去 Python 依赖。** 参考 NOOMD，桌面应用启动不依赖独立后端服务。
2. 日常开发在项目根目录执行 `npm run tauri` 即可启动，方便用户持续查看和验收修改。首次安装依赖与工具链准备另行说明。
3. AI 接入参考 NOOMD：既能自己配置模型服务，也能使用本地 AI CLI。
4. 浏览器或电脑控制参考主要厂商的实现方式，避免账号备份，优先选择权限受限、可见、可撤销的安全控制方式。
5. 先对齐任务、列出计划，再由 Luna 执行。任务、决定和进度必须留在仓库文档，支持跨会话与中断恢复。

## 2. 本文权威与现有文档关系

本文记录本次变更目标、待决事项和恢复检查点。未确认的建议不等于用户已批准。

- `docs/前端-selector-与-tauri-command-改造方案.md` 曾提出保留 AI、爬虫、发布的 Python 后端；该目标与本次去 Python 方向冲突，不得直接作为本任务的执行方案。
- `docs/整体技术架构与前端去接口化分析.md`、`docs/接口去留清单.md` 可作为现状线索，必须对照当前代码复核。
- `docs/开发参考/旧版工程运行说明.md` 中的 Python/FastAPI 启动说明描述旧实现；不能据此将 Python 固化为新架构要求。迁移完成时同步更新相关文档。

## 4. 待对齐事项

| 编号 | 问题 | 当前状态 |
| --- | --- | --- |
| D1 | 是否接受 P0 先交付独立启动和本地核心功能，采集与自动发布暂时不可用？ | 仍未明确同意下线；P0 不删除或静默禁用采集/发布入口，未迁移能力保持现状并在交付中明确其依赖 |
| D2 | 旧 Python CLI、MCP 的功能是否需要迁移保留，还是退出本次产品交付？ | 本任务不删除、不改语义；桌面启动退出 Python 依赖与 CLI/MCP 是否另行迁移分开处理 |
| D3 | 首批支持哪些本地 AI CLI，需不需要图像输入等能力？ | 参考 NOOMD 形成 `claude`/`codex`/`opencode` 候选和 provider 配置轴；首批具体能力、图片支持和命令契约待 P1 设计验证 |
| D4 | 浏览器采用扩展、独立会话或系统控制？ | NOOMD 仅验证扩展 Native Messaging 剪藏边界；本项目候选优先扩展/Native Messaging，XHS 控制和权限范围待调研实测，暂不锁定 |

`npm run tauri` 是用户指定的启动体验，不自动意味着改用 npm 安装全部子项目依赖或替换现有 pnpm 锁文件。

## 6. 执行边界与停止条件

- 已收到用户“执行以上”授权，可在同一 CHG 内修改实现；每阶段必须先更新检查点，不能用旧的“仅写文档”状态阻挡已授权的 P0。不得自动提交或推送。
- 不修改 NOOMD、移动端或 MediaCrawler 子模块；不提交 `.env`、账号会话、数据库、素材等用户数据。
- 不覆盖其他会话的未提交修改。文档建立时观察到 `tools/MediaCrawler` 子模块脏状态和未跟踪 `.noomd/`，均不属于本任务。
- 如需删除既有功能、破坏性数据迁移、扩大电脑控制权限或改变已确认范围，先记录具体冲突与可选方案，再对齐。
- 构建/类型检查通过不代表桌面交互或浏览器发布已验收；证据必须标明验证层级。

## 阶段 A 结论

- 已完成当前 Tauri/React/Python 路径、SQLite schema、路由覆盖、CLI/MCP 与打包入口盘点，并已写入[技术设计与执行计划](./00-02-技术设计与执行计划.md)的逐功能迁移表。
- NOOMD 对照确认了 Rust command/SQLite、受限本地 CLI 和 Native Messaging 的可借鉴边界；其 Markdown 工作空间实现不直接适用于本项目 `data/app.db`。
- P0 启动切片与本地最小数据通路已通过：根目录单命令启动、移除 Python child/健康门、Rust SQLite 读取 profile/items/notes 并创建本地草稿；下一步逐页接入本地核心 CRUD，不以删掉采集/发布入口代替迁移。


## 文档影响

验收后逐项判断：产品事实、技术事实、发布说明、用户手册、完成记录。当前不宣称迁移完成。

## 阅读顺序

- [技术设计与执行计划](./00-02-技术设计与执行计划.md)
- [实施与验证记录](./00-03-实施与验证记录.md)
- [V2.8 桌面 UI 与交互优化计划](./00-04-V2.8桌面UI与交互优化计划.md)（方案已整理，供后续 LUNA 执行；本轮未实施 UI）
