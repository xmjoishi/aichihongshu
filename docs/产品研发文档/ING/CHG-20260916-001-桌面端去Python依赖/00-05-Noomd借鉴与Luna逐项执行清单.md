# Noomd 借鉴与 Luna 逐项执行清单

任务：CHG-20260916-001。建立日期：2026-09-16。执行者：Luna；产品验收：用户。

用户要求：“任务持久化一下，我让 Luna 挨个执行”。本清单作为逐项执行入口；N00–N13 已完成各自的最小安全切片，当前进入 N14 自动回归与文档收口。原 P0 授权持续有效，不重复确认。D3/D4 中尚未确定的 CLI 能力、浏览器控制权限仍需由对应设计与实测解决，不解释成全量授权电脑控制、正式发布或删除功能。

## 1. 目标、基线与文档关系

目标：借鉴 Noomd 的本地保存、上下文隔离、AI 提案、运行管理、来源追踪和工程边界，使爱吃红薯形成“选素材 → 编辑/AI 生成 → 采用 → 保存 → 重启恢复 → 发布准备”的可靠桌面创作流程。

- 只读调查基线：`42a3729d77a30d69fd26a2536dde56b97ca5e111`。执行前重新读取 HEAD 和工作区差异，不回退到此提交。
- 调查时无关脏状态：`tools/MediaCrawler` 子模块、未跟踪 `.noomd/`。不得清理或纳入提交。
- 当前已有：Tauri 独立启动、Rust SQLite 本地读取、新建草稿、部分本地媒体读取、主题/侧栏优化。
- 当前未完成：完整 AI 替代、采集与发布迁移、发布版历史数据导入、本地浏览器控制和平台资料抓取；本地账号池、人设、灵感和榜样账号手工元数据已落地，仍需原生验收。笔记删除已按“移入账号回收站、关联素材保留、可恢复”落地，素材回收站支持账号边界下的批量永久清理；图库/笔记回收站的原生操作验收仍待用户执行。
- 代码线索：`Notes.tsx` 将本地编辑设为只读；旧自动保存失败被静默忽略；`useAIStream.ts` 使用 sessionStorage 且切笔记时缺少旧运行隔离；本地页面查询键与账号切换失效列表不一致；`LocalImage.tsx` 的 Map 缓存无容量及图片版本。这些是静态观察，Luna 必须复核并用针对性用例验证，不把风险写成已复现故障。
- 本文细化 [技术计划](./00-02-技术设计与执行计划.md)；[UI 计划](./00-04-V2.8桌面UI与交互优化计划.md) 的 U3 依赖本文本地保存闭环。不得重复实现已完成 U1/U2，不重做品牌和侧栏。
- 本文维护任务状态；实际实施、测试、AC 结果和恢复检查点统一写入 [实施记录](./00-03-实施与验证记录.md)，不复制第二份结果正文。N00 的矩阵和 QA 入口详见 [N00 基线能力矩阵与隔离 QA](./00-06-N00-基线能力矩阵与隔离QA.md)。

## 2. 统一执行约定

1. 按任务编号顺序执行，每次只承担一个明确任务；先核对前置依赖和最新代码，完成后记录下一项。前置实现已验证而产品验收待用户时，可推进不改变其未决语义的独立任务，但不得将整体标为验收完成。
2. 默认 Read Set：AGENTS、导航、协作规则、当前 CHG、对应 `client/src`/`client/src-tauri`/Python 旧契约。Noomd 仅作只读参考，当前源码优先于可能过期的文档。
3. 默认 Write Set：本任务列出的桌面模块、必要测试、构建配置和本 CHG 文档。新模块按职责创建；开始前记录精确文件范围。不得顺带修改移动端、官网、品牌源、Noomd 或 MediaCrawler。
4. 测试使用隔离数据库、素材及两个测试账号。Tauri 开发态当前可能直连项目 `data/app.db`，必须先建立可验证的测试路径选择，不在用户正式库写测试记录。
5. 保留旧 Python CLI/MCP 和未迁移功能语义。不通过删除入口、伪造成功或回落为无提示空结果完成迁移。若修改仍在使用的 Python 后端，按工程说明重启对应服务。
6. 数据变更使用兼容迁移、明确版本、事务及失败恢复；备份覆盖数据库和素材引用。需要破坏性迁移、正式数据写入、扩大系统权限或平台提交时，只暂停对应动作，说明具体决策和替代方案。
7. 默认由 Luna 执行自动测试，用户进行产品操作验收。用户明确委托真实操作后，Luna 使用隔离资料做 Tauri 验收；浏览器不代替原生文件选择、关闭及重启。
8. 每项开始和结束更新检查点。不自动提交、推送或发布；构建成功不等于产品验收。

## 3. 任务总表

| 编号 | 任务 | 依赖 | 当前状态 | 交付门槛 |
| --- | --- | --- | --- | --- |
| N00 | 基线、能力矩阵与隔离 QA 入口 | 无 | 已实施；AC1/AC2 脚本与静态验证通过，原生桌面 QA 待补 | 本地/旧服务/未实现能力与测试数据边界可核对 |
| N01 | 账号上下文与异步隔离 | N00 | 已实施；账号池本地写入、代码/单测通过，原生 A/B 竞态验收待补 | 查询、草稿、任务不会跨账号回灌 |
| N02 | 本地笔记 CRUD 与可靠保存 | N01 | 已实施；字段保存、版本校验、Markdown 导出和本地笔记回收站/恢复通过自动验证，原生闭环待验收 | 编辑、关联、保存、切页、删除恢复、重启回读 |
| N03 | 素材导入、关联与缓存生命周期 | N02 | 已实施本地导入/关联/缓存、原图/缩略图分工、元数据编辑、图片修复、批量缺失扫描、软删除/恢复和回收站批量永久清理；原生三图验收待补 | QA 素材关联/排序、失败边界、引用保护、缓存更新 |
| N04 | 数据位置、历史导入与可恢复备份 | N03 | 已实施隔离预检/备份恢复演练和设置页可见命令入口；正式写入仍需人工选择 | 发布版显式导入与隔离恢复验证 |
| N05 | Rust 业务边界与能力驱动 UI | N04 | 已实施最小 capability 矩阵与图库/笔记接入；原生验收待补 | 页面不以 Tauri 身份推断全部能力 |
| N06 | API Provider 与本地 CLI 接入 | N05 | 已实施 CLI 探测、非交互文本调用、stdout 事件和子进程取消；认证/图片/工具能力待验收 | 真实连接与用途能力矩阵 |
| N07 | AI 运行与会话持久化 | N06 | 已实施本地 CLI 事件接入、runId 隔离、取消、SQLite 运行元数据和助手文本产物；真实 Tauri 重启/产物回读待验收 | 运行可取消、可追踪、结果隔离 |
| N08 | AI 修改提案与来源上下文 | N07 | 已实施提案版本冲突、来源边界、前后差异视图和同作用域本地提案历史；真实来源待补 | 逐字段采用、冲突保护、来源可见 |
| N09 | 单篇笔记创作台 | N08 | 已实施最小当前账号/保存状态/辅助面板边界；原生尺寸验收待补 | 素材、正文、AI 在同一对象内协作 |
| N10 | 灵感收集与选题转化 | N09 | 已实施 SQLite 本地书签/观察、账号隔离、去重与显式转草稿；真实来源抓取待补 | 参考有来源，转草稿有显式关联 |
| N11 | 搜索、业务视图与导航恢复 | N10 | 已实施最小统一搜索与视图偏好；原生重启/大样本性能待验收 | 当前账号搜索、筛选/滚动恢复 |
| N12 | 浏览器收集适配 | N11 | 已实施剪藏消息校验/去重契约；真实浏览器链路依赖 D4 | 先验证受限收集，后接正式入口 |
| N13 | 发布准备、填充与结果核查 | N12 | 已实施内容准备检查与发布快照/幂等契约；平台填充与提交依赖 D4/另行授权 | 发布状态含结果不明且不重复提交 |
| N14 | 全链回归与文档收口 | N13；可先回归已完成阶段 | 已完成自动回归与文档收口；原生 Tauri、D3/D4 和正式平台链路仍待验收 | AC 逐条记录，真实未完成项保留 |

建议分批验收：第一批 N00–N04；第二批 N05–N08；第三批 N09–N11；第四批 N12–N14。出现 D3/D4 阻塞时保留阻塞原因，可继续不依赖该能力的工作，不制造虚假的完成状态。

本轮代码收口补充：Tauri 设置页回收站已改为本地账号快照和 Rust 恢复/永久清理 command；本地发布确认只有状态写入成功才关闭弹窗。浏览器运行时仍保留旧 HTTP 回收站与发布路径。完整自动检查已通过，原生桌面操作、真实 CLI 登录态、浏览器扩展传输、正式历史库导入和平台提交仍是产品验收边界。

## 4. 逐项任务卡

### N00 基线、能力矩阵与隔离 QA 入口

- 入口：`client/src/App.tsx`、`lib/{api,local}.ts`、各页面、`src-tauri/src/{lib,db}.rs`、`client/package.json`、旧 Python routers/schema。
- 工作：复核已实现读写与 UI 入口，形成操作级能力矩阵；为 QA 建立独立数据目录/数据库和素材选择机制，默认生产路径保持兼容；建立 A/B 两账号的固定测试样本和复位说明。输出后续任务对应的代码入口及必要检查命令。
- 非目标：不重跑历史 UI 改版，不自动打开或改写用户数据库，不以全面框架重构开场。
- N00-AC1：启动隔离 QA 时显示或日志可核对实际路径；测试写入与生产数据目录分离，退出重开仍使用 QA 位置。
- N00-AC2：每项能力标明本地可用、旧服务依赖、未实现或待验证；读取可用不等于保存可用。
- 实施结果：详见 [N00 基线能力矩阵与隔离 QA](./00-06-N00-基线能力矩阵与隔离QA.md)。已增加固定 QA workspace、A/B 两个 operation 测试账号、两条图片/草稿样本、显式 `AICHIHONGSHU_QA_WORKSPACE` debug 路径和 `npm run tauri:qa` 入口；生产 `data/app.db` 与 `assets/` 保持不触碰。
- 自动验证：`npm run qa:prepare -- --json` 可重复执行并输出 QA workspace/database 路径；`npm run qa:reset` 只复位固定 `.qa/desktop-workspace`；SQLite 计数为 2 账号、A/B 各 1 item/1 note、active=101。
- 遗留：真实 Tauri 原生窗口显示路径、关闭/重开 UI 回读和后续页面竞态仍待 N01/N02 及用户产品验收；N00 不把脚本/构建通过写成全链产品验收。

### N01 账号上下文与异步隔离

- 入口：`ActiveAccountSwitcher.tsx`、本地查询/页面、`useAIStream.ts`、`LocalImage.tsx`、Rust 账号范围检查。
- 工作：建立显式账号上下文；查询键包含账号和必要筛选；切换时处理旧请求、选中项和未保存草稿；请求/任务捕获发起账号与对象，Rust 写入验证对象归属。复核目前 `local-*` 查询失效不一致，不仅补一处列表。
- 缓存键规则：数据库身份＋账号＋对象＋必要版本；UI 过期响应检查与 Rust 数据隔离分别负责，前端隐藏不替代后端检查。
- 非目标：不修改运营号/辅助号含义、不复制账号登录态。
- N01-AC1：A 列表请求延迟时切 B，A 返回不覆盖 B；图库、笔记、人设、榜样、数据和灵感均核查。
- N01-AC2：A 发起保存或 AI 后切 B，结果仍归 A，B 不出现旧草稿/选中/输出；越权对象 ID 写入失败且无数据变化。

- 实施结果：新增 `client/src/lib/accountContext.ts`，统一解析数据库身份、当前 operation 账号和作用域 key；账号切换成功后由 `ActiveAccountSwitcher.tsx` 发出清理事件。Dashboard、图库、笔记、人设、榜样、数据和灵感的本地查询键均包含账号作用域及页面筛选；切换回调清理选中项、运行态和未保存编辑态。`useAIStream.ts` 记录账号/对象/数据库身份/版本隔离的会话键，并在账号或对象切换时取消旧流、拒绝过期回调；`LocalImage.tsx` 使用同样维度的图片缓存键。Rust `read_status`、`read_local_image`、`create_local_draft` 接收期望账号并在读取/写入前校验归属。
- 自动验证：`npm run build`、`cargo fmt --manifest-path client/src-tauri/Cargo.toml -- --check`、`cargo check --manifest-path client/src-tauri/Cargo.toml`、`cargo test --manifest-path client/src-tauri/Cargo.toml db::tests`（4 passed）和 `git diff --check` 通过；静态核对确认七个本地页的 query key 均带 `scopeKey`，AI/图片回调有序列或取消保护。
- AC 状态：N01-AC1 已通过代码/构建静态验证（延迟响应在账号事件或作用域变化后被丢弃）；N01-AC2 的 Rust 越权账号读/写拒绝且测试确认数据不变，前端保存/AI 旧回调隔离已落地。真实 Tauri QA-A/QA-B 原生窗口操作、保存/AI 途中切换和关闭重开尚未执行，不能标记产品验收通过。
- 未改变：旧 Python CLI/MCP、远端 API 契约和运营/辅助账号语义保留；未写正式 `data/`、`assets/`、`.env`、移动端、Noomd 或 MediaCrawler。

### N02 本地笔记 CRUD 与可靠保存

- 入口：`Notes.tsx`、`BodyEditor.tsx`、`lib/local.ts`、Rust 笔记模块；对照旧 content API。
- 工作：迁移现有笔记编辑、标签、类型、图片关联/顺序及既有状态动作；手动、自动与快捷键保存进入同一队列。使用内容版本或等价乐观并发校验；后端原子校验再更新，前端旧回执不清除新版本 dirty。保存失败持续显示并保留输入；切页、切账号、原生关闭提供离开保护。
- 保持纯文本正文和旧数据兼容；读取失败不进入可覆盖保存的空编辑器。删除沿用核实后的现有语义；若需新增回收站或改变永久删除语义，先记录设计决定，不暗改。
- N02-AC1：编辑标题/正文/标签/图片顺序 → 保存 → 切页 → 关闭重开，内容一致；失败重试不丢输入。
- N02-AC2：第一次保存延迟期间继续编辑，旧回执不能显示当前已保存；两个入口更新同一笔记时识别版本冲突。
- N02-AC3：修改后立即切页、切账号、关闭原生窗口，均有正确保存/保留/取消路径；标记待发布与用户确认已发布保持原语义。

- 实施结果：`Notes.tsx` 在 Tauri 本地模式下恢复标题、纯文本正文、标签和可用发布类型编辑；手动保存、自动保存和 `Ctrl/Cmd+S` 共用串行保存队列，保存回执按账号、笔记和 epoch 校验。`lib/local.ts` 新增本地笔记编辑/状态 command 适配；Rust `db.rs` 新增带 `account_pool_id`、`expected_version` 的事务更新，校验当前账号、允许的状态/类型和素材归属，并递增 `content_version`。保存失败保留编辑态并显示“保存失败，可重试”，保存按钮可再次提交。
- 图片关联/排序：Rust command 已支持受当前账号校验的 `item_ids`，但现有 `NoteImageStrip`/`LibraryPicker` 仍是旧 HTTP 交互；Tauri 本地调用已设为只读并显示后续接入提示，避免误发 `/api/content` 写请求，待 N03 接入本地素材生命周期后再开放。
- 自动验证：`cargo test --manifest-path client/src-tauri/Cargo.toml db::tests` 4 passed，覆盖字段更新、版本递增、旧版本回执拒绝且不覆盖、状态更新、账号越权拒绝、历史库迁移；`npm run build`、`cargo fmt --manifest-path client/src-tauri/Cargo.toml -- --check`、`cargo check --manifest-path client/src-tauri/Cargo.toml`、`git diff --check` 通过。
- AC 状态：N02-AC1 的本地字段保存、版本回读、状态写入和 Markdown 导出通过隔离 Rust 单测/编译及 `check:note-export`；图片顺序、切页/关闭重开 UI 未完成原生验收。N02-AC2 的乐观版本冲突和串行队列已通过单测/静态检查；真实延迟回执操作待 QA。N02-AC3 的账号 epoch 清理、失败保留和“标记待发”本地 command 已落地；原生离开保护、重启回读和正式发布确认仍待用户验收。
- 未改变：旧 Python CLI/MCP、远端 `content` API、发布确认语义和正式数据均保留；未写 `data/`、正式 `assets/`、`.env`、移动端、Noomd 或 MediaCrawler。

### N03 素材导入、关联与缓存生命周期

- 入口：`Library.tsx`、Notes 素材选择器、`LocalImage.tsx`、Rust 文件与 items 业务模块。
- 工作：本地导入、元数据编辑、现有删除/恢复语义和笔记关联；稳定素材 ID；原图/缩略图分工；有上限的缓存、图片版本失效、重复请求合并。校验允许的文件类型、大小及路径范围，处理符号链接；记录大小、类型、摘要等必要信息。
- 文件和数据库无法共用同一事务：设计暂存 → 文件落位 → 数据提交及失败补偿；不留下标成成功的断链素材。不顺带自动清理孤儿文件或重复素材。
- N03-AC1：导入三图 → 建笔记 → 排序/保存 → 切页/重启，原图和顺序可回读。
- N03-AC2：模拟文件失败和 DB 失败，可恢复或有明确残留记录；替换图片后各视图不显示旧缓存；跨账号不可读取。
- N03-AC3：固定规模样本记录列表滚动、首次显示与内存表现；缺失素材可定位问题并重新关联，不无限重试。

- 实施结果：新增 `update_local_note_items` 本地 command，按 active account、笔记 `content_version`、素材存在性和归属进行事务校验，写入有序 `item_ids` 与首图；随后补充 `import_local_image`、`repair_local_image` 和 `create_local_draft_from_items`，完成图片类型/大小/魔数/MIME 校验、SHA-256 摘要、账号素材目录落位、暂存文件补偿、同内容去重、缺失文件修复、图片版本递增/分析失效及从素材创建有序关联草稿。导入/修复时前端生成 512px JPEG 缩略图，Rust 将原图和缩略图按摘要分目录落位，旧素材无缩略图时回退原图；图库卡片读取缩略图，详情读取原图。工作区快照现在按账号批量计算 `missingImageIds`，图库对缺失文件显示数量和“文件缺失”遮罩，详情给出修复指引；回收站详情同时提示关联笔记数量，并支持批量恢复。`Notes.tsx` 的 `NoteImageStrip` 在 Tauri 下通过本地回调执行排序/移除，图片详情直接取 `localWorkspace.items`，不再因旧 `/api/library/:id` 读请求为空。`LocalImage.tsx` 增加 pending 请求合并和 128 项 LRU，缓存键保留数据库身份、账号、素材变体、图片版本/内容摘要。图库详情提供“修复/替换图片”，旧文件保留避免误删共享引用。
- 自动验证：`npm run check:local-import`、`cargo test --manifest-path client/src-tauri/Cargo.toml db::tests -- --test-threads=1` 6 passed，覆盖有效图片、内容去重、素材草稿、缺失图片修复/版本冲突、非法文件名/魔数、账号错配、素材顺序保存、首图同步及跨账号关联拒绝；`npm run build`、`cargo fmt --manifest-path client/src-tauri/Cargo.toml -- --check`、`cargo check --manifest-path client/src-tauri/Cargo.toml`、QA reset/prepare 和 `git diff --check` 均通过。
- AC 状态：N03-AC1 的本地导入→素材草稿→有序关联 command 已通过 Rust 单测/构建验证，真实三图原生操作及重启回读待验收；N03-AC2 的账号边界、SHA-256 版本键、原图/缩略图变体、重复请求合并、内容去重、缓存上限和替换后的摘要刷新已落地；N03-AC3 的类型/大小/MIME/魔数错误、缩略图校验、暂存失败补偿、归属拒绝、修复版本冲突和快照批量缺失扫描已覆盖，真实断链修复、列表性能和文件选择尚未验证。
- 遗留：回收站永久批量清理仍未实现；回收站详情已提示关联笔记数量，避免用户误以为可以直接丢弃；旧 Python API/MCP 保留。

### N04 数据位置、历史导入与可恢复备份

- 入口：Rust 数据路径解析/迁移、设置页、文件导入导出适配；对照旧 schema。
- 工作：区分开发库、发布版库、QA 库；提供用户可见的历史库导入和检查预览，覆盖素材依赖。保留源库；迁移前备份；校验账号映射、记录数量、关键字段与图片可达性。已有目标数据不得静默覆盖；合并/替换语义不明确时只完成预检并列出具体选择。
- 备份清单排除凭据、Cookie、浏览器 profile；有外部素材时明确包含、复制或缺失范围。恢复先在独立目录验证，不覆盖正在使用的库。
- N04-AC1：隔离历史样本导入后数量、账号归属、正文、图片一致，原样本未改写。
- N04-AC2：不兼容 schema、目标非空、缺失素材和中断均给出准确结果；恢复副本可启动并读回，不把“备份文件存在”当作可恢复。

- 实施结果：新增 `scripts/desktop-db-safety.mjs` 及根命令 `db:preflight`、`db:backup`、`db:restore-check`、`db:import-plan`。预检只读 SQLite `quick_check`、schema 表/列、账号映射、孤儿记录、关键计数和 `items.image_path` 可达性；拒绝绝对路径、越界路径、符号链接和非普通素材文件。`db:import-plan` 只读比较源库与目标状态，明确 replace/merge/cancel 三种选择并默认 cancel，不复制或覆盖任何数据。备份使用 SQLite `.backup`，在 `output.staging-<pid>` 完成后原子改名，清单只包含数据库和被引用素材，明确排除 Cookie、浏览器 profile、凭据、`.env` 和未引用文件。恢复复制到独立暂存目录，重新检查 schema、计数、账号和素材哈希后才落位，不覆盖当前库。Tauri 设置页新增“本地数据迁移与恢复”区，显示当前数据库路径，要求显式填写源库/素材/目标/备份/恢复路径，并提供可复制的只读预检、备份和恢复校验命令；正式导入保持阻断，界面明确需要人工选择且不会静默覆盖。
- 测试样本：固定 `.qa/desktop-workspace`（QA-A 101、QA-B 202）作为源，备份输出 `.qa/n04-backup`，恢复输出 `.qa/n04-restore`；源数据库未修改，正式 `data/`/`assets/` 未作为写入目标。
- N04-AC1 实际结果：通过隔离 QA 演练；源/备份/恢复均为 2 accounts、2 items、2 notes、2 profiles，A/B 各 1 item/1 note、无孤儿，两个引用图片大小与 SHA-256 一致；manifest 记录账号归属和引用路径，源 `sourceUnchanged=true`。
- N04-AC2 实际结果：通过预检边界演练；非空目标返回 `TARGET_NON_EMPTY`，空素材根返回 `missing_assets`，缺表/缺列样本返回 `incompatible_schema`，均退出码 2；`db:import-plan` 在隔离 QA 库上返回 `review_required`、`writes=false` 和三种选择；备份/恢复使用暂存目录并在成功后生成独立目录。未模拟进程强杀后的自动清理，但失败保留暂存目录并不给成功结果；未进行真实 Tauri 恢复后窗口启动。
- 未开放范围：正式库历史导入、合并/替换写入和发布库迁移仍待明确目标选择与用户验收；设置页的预检/备份/恢复命令入口已可见。旧 Python CLI/MCP、旧 schema/HTTP 契约保留。

### N05 Rust 业务边界与能力驱动 UI

- 入口：`lib.rs`、`db.rs`、`lib/{local,api}.ts` 和页面能力分支。
- 工作：按账号/素材/笔记/任务拆分业务责任，commands 保持薄层；同一业务契约由本地适配承接。必要时抽共享 crate，但不为抽象而大规模搬迁。用操作级 capability 描述读、写、AI、收集、发布及依赖原因，替换 `IS_TAURI_RUNTIME => 一律只读` 的业务推断。
- 增加适量架构检查，约束业务核心不依赖桌面 UI。保留旧 CLI/MCP，不在此任务重写其全功能。
- N05-AC1：某能力可用时入口可操作；不可用时原因和下一步准确；不出现可点击却必失败的假入口或静默 HTTP 回退。
- N05-AC2：本地核心测试可独立运行，重构前后契约和历史样本结果不变。

- 实施结果：新增 `client/src/lib/capabilities.ts`，以 `CapabilityId`/`CapabilityState` 描述 `note.read`、`note.write`、`note.items.write`、`library.read`、`library.import`、`library.repair`、`note.export`、`ai.generate`、`collect`、`publish`，并补充状态写入、删除、分析等具体操作；不可用项必须带 `reason` 和 `nextStep`。浏览器预览返回 `legacy-http` transport，本地 Tauri 返回逐操作 local 状态。
- 页面接入：`Library.tsx` 的导入、AI 分析、删除、由素材建稿及多选操作按 capability 禁用并显示原因；`Notes.tsx` 的新建、字段保存、状态推进、素材关联、AI、导出、删除和发布分别按 capability 处理，已迁移的本地笔记保存/状态/素材关联不再被 Tauri 身份一刀切禁用；本地状态列表推进使用已有 Rust command，浏览器仍走旧 HTTP。
- N05-AC1 实际结果：通过静态检查和构建核对；本地笔记读写、状态、素材关联、素材导入/修复、从素材建稿和 Markdown 导出可用，AI、收集、发布、删除等未迁移入口仍按 capability 禁用并有原因/下一步；未验证真实 Tauri 点击后的原生提示呈现。
- N05-AC2 实际结果：`npm run check:capabilities`、`npm run check:local-import`、`npm run check:note-export` 和 `npm run build` 通过；未改旧 Python CLI/MCP 或 QA 样本，Rust DB 6 项测试通过。
- 未开放范围：本地 AI Provider/CLI、灵感/浏览器收集和发布仍由后续任务决定；本地素材软删除/回收站恢复、原图/缩略图读取和 Markdown 复制已开放，剪贴板权限、复杂内容及原生图库/回收站操作仍待验收。

### N06 API Provider 与本地 CLI 接入

- 入口：Settings、AI 配置模型、Rust 设置/凭据/Provider/CLI 适配器；Noomd `app_settings.rs`、`agent_cli.rs` 只读参考。
- 工作：配置与凭据分离；模型服务、本地 CLI、用途默认值分区。先逐个验证候选 `claude`/`codex`/`opencode` 的检测、认证、流式、文本、图片、工具及取消能力，记录支持矩阵后启用相应入口；不宣称三者等价。
- D3 处理：每个适配器先记录真实版本与最小命令契约；无需重复确认已授权接入方向，但未经验证的图片/工具能力不得上线。CLI 安装/登录缺失给出明确状态，不自动改用户全局 CLI 配置或购费。
- UI 不返回完整密钥；日志脱敏；API key 不写 sessionStorage、业务库、备份或文档。Noomd 的应用 vault 不等同于系统钥匙串，按实际实现说明边界。
- N06-AC1：保存配置、重启、列模型、文本调用成功；无密钥、无认证、模型不可用和网络失败分别显示正确原因。
- N06-AC2：图片任务只能选择经过验证支持图片的入口；每个启用 CLI 有实际输出与取消证据，缺失项明确不可用。

### N07 AI 运行与会话持久化

- 入口：`useAIStream.ts`、AIPanel、Rust AI 运行/任务存储模块。
- 工作：会话、运行、产物分离；记录 run ID、发起账号、笔记、模型、输入资源引用、状态、错误和产物关联；聊天历史使用本地持久化，限制上下文大小。运行流统一事件并隔离过期回调；取消传递到实际 provider/子进程，定义任务清理与超时。
- 第一版重启恢复历史与产物；上次运行中的任务标为中断并允许显式重试，不自动续跑、不自动再次发布。重试建立新运行且链接原任务。用户数据和提示词的保留范围要明确，避免把全量敏感输入写进诊断日志。
- N07-AC1：生成中切笔记/账号，结果不串；停止后实际任务结束或准确显示取消待确认，不仅关闭 spinner。
- N07-AC2：应用重开可找回会话/产物；中断状态真实；重试不覆盖已采用内容，任务失败保留错误和可执行下一步。

### N08 AI 修改提案与来源上下文

- 入口：AIPanel 的标题/标签/替换/追加回调、笔记保存层、AI 工具定义与来源组件。
- 工作：复用已有采用入口，增加提案 ID、目标账号/笔记、原始版本、建议字段及采用状态。标题、正文、标签分别采用；正文显示差异。采用到草稿和落盘保存分开反馈；版本冲突不覆盖。
- 显示本次选用的人设、素材、参考和经验规则；区分用户确认事实与模型推测。参考只有 URL 没正文时明确不可据其生成摘要；引用来源从真实工具结果生成，不由模型编造。检索/读取工具限制当前账号及选定资源，外部正文不作为工具授权指令。
- N08-AC1：生成后手动修改正文，再采用旧提案出现冲突且原文保留；仅采用标题不改变正文和标签；采用后保存失败不显示已落盘。
- N08-AC2：所有显示来源可定位；缺正文的链接不产生伪摘要；未确认的材质/价格等推测与确认事实可区分。

### N09 单篇笔记创作台

- 入口：Notes、素材选择/预览、AIPanel、现有面板尺寸 hook 和 UI 基础组件。
- 工作：围绕同一笔记组织素材抽屉、正文、可收起 AI 面板；顶部显示当前账号、保存和业务状态。窄窗口按需收起辅助区域，保留既有主题、品牌、侧栏偏好和可访问性。
- 不引入 BlockNote，不改纯文本正文，不强推固定三栏，不重做整套设计系统。
- N09-AC1：从图库选素材开始创作，选图、人设、AI 会话始终属于当前笔记；生成/比较/采用/保存无需反复丢失上下文。
- N09-AC2：按 UI 计划窗口尺寸、深浅主题、键盘操作验证；辅助面板不会遮挡主保存操作；重开能回到有效对象。

### N10 灵感收集与选题转化

- 入口：Inspire、KnowledgeTab 的真实调用路径、Rust 参考资料/选题模块。
- 工作：先支持手工文本或链接收集，保存标题、来源、时间、正文获取状态、用户收藏理由和素材关联；参考资料与自己的创作意图区分。显式“转成草稿”后记录目标笔记关联，防止重复点击创建多份。
- 复用现有知识/经验存储前核实语义，避免再造一套重复知识库；不自动把收藏写成自己的已完成内容。
- N10-AC1：收集 → 补充观察 → 关联素材 → 转草稿 → 重开，关联及来源完整；转化成功前不误标已采用。
- N10-AC2：A/B 隔离、空正文、删除源资料后的历史引用均有明确处理；缺内容时允许保留书签，但不伪称快照完整。

### N11 搜索、业务视图与导航恢复

- 入口：图库/笔记/灵感列表、Rust 查询、前端导航与偏好存储。
- 工作：当前账号统一搜索素材、笔记、灵感、已存在经验；结果包含类型/摘要/跳转。图库增加必要的画廊/紧凑列表，笔记支持现有状态分组；保留账号维度筛选、排序、滚动位置。分页/索引按样本测量，不每个页面重复拉取全库快照。
- 第一版不做向量库、任意属性数据库、复杂多页签；多视图共享同一份业务数据。日历仅在计划日期字段语义明确后扩展，不能拿创建时间代替发布时间。
- N11-AC1：搜索不返回另一账号内容；打开结果再返回保留筛选和位置；重启后偏好正确隔离。
- N11-AC2：各视图统计一致；拖动/修改状态遵守现有业务契约，不能拖入“已发布”就伪称平台发布成功。

- 实施结果：新增 `client/src/lib/workspaceSearch.ts` 与 `/search` 页面，统一搜索当前作用域内的笔记、素材、灵感和榜样资料，结果带类型、摘要、状态和可跳转链接；侧栏恢复“搜索”入口。图库支持从搜索结果打开指定素材，并按数据库/账号保存标签、未识别筛选、页码、列数和滚动位置；笔记列表按同一作用域保存状态、关键词、排序和滚动位置；榜样结果支持通过 URL 参数打开详情抽屉。
- 自动验证：`node --check scripts/check-workspace-search.mjs`、`npm run check:workspace-search`、`npm run check:creator-workspace`、`npm run check:inspiration-capture`、`npm run build`、`git diff --check` 通过；构建保留既有大 chunk 提示。
- AC 状态：N11-AC1 的作用域键、搜索输入和结果跳转已落地，静态检查确认没有跨账号回退；视图偏好按 `scopeKey` 隔离并恢复，真实 Tauri 重启/切账号操作待验收。N11-AC2 保留原图库/笔记状态契约，未新增平台发布假成功；统一搜索当前使用已加载快照，尚未做大样本分页/索引测量。
- 未开放范围：不做向量检索、任意属性数据库、复杂多页签或日历；浏览器搜索继续使用现有 HTTP 列表，灵感仅在本地模式纳入，待后续收集能力统一后再扩展。

### N12 浏览器收集适配

- 入口：后续 integrations 浏览器扩展/宿主适配、N10 收集服务；Noomd 扩展和 Native Messaging 只读参考。
- 工作：先记录 D4 方案对照和最小原型实测，优先验证当前页受限收集；只有所需权限/安装路径明确后接正式入口。记录标题、来源、时间、正文及目标账号；宿主重新校验消息大小、路径、目标身份和内容，不盲信扩展输入。
- 不导出 Cookie、浏览器 profile 或账号备份；不直接执行 Chromium 二进制；不把 Native Messaging 本身当作已证明的小红书发布方案。
- N12-AC1：用户选择当前页和目标账号后准确入库；未授权目标、超限内容、失败重试不产生错误归属或无提示重复。
- N12-AC2：真实浏览器链路记录支持浏览器/版本、权限与失败反馈；需要新增持久授权时先明确具体范围。

- 实施结果：新增 `client/src/lib/browserCapture.ts`，为手工/扩展剪藏定义统一消息契约；宿主在入库前重新校验目标账号、标题、`http(s)` 来源、时间、字段长度和 128KB 总消息上限，并生成当前账号维度的去重键。`Inspire.tsx` 的本地手工保存复用同一校验，再通过 Rust SQLite command 持久化；非法来源、账号变化、重复转换和跨账号灵感 ID 会明确失败，不写入错误归属。
- 自动验证：`node --check scripts/check-browser-capture.mjs`、`npm run check:browser-capture`、`npm run build`、`git diff --check` 通过；未调用真实浏览器、未申请新权限。
- AC 状态：N12-AC1 的消息格式、账号归属、大小和 URL 边界已通过静态检查，真实扩展传输/失败重试待 D4 方案与原生链路验收。N12-AC2 尚未声称浏览器支持矩阵，扩展/Native Messaging 仍未接入。
- 未开放范围：不导出 Cookie、profile 或账号备份；不直接执行 Chromium；不把剪藏校验契约当作浏览器链路已完成。

### N13 发布准备、填充与结果核查

- 入口：Notes 发布助手、现有发布 API 契约、浏览器适配与任务记录。
- 工作：先实现内容检查/导出/发布准备，再在 D4 已解决且实测后接填充。区别准备完成、填充完成、已发起提交、已确认成功、失败、结果不明；保留用户手动确认已发布的独立语义及来源。
- 提交前核对本地账号与浏览器账号；为发布尝试建立稳定标识和重复提交保护。超时进入待核查，不自动再次提交。发布中的内容使用固定版本快照，后续编辑不能悄然替换已提交对象。
- 正式账号发布不属于本计划默认授权；不具备测试环境时完成契约/自动测试/准备动作，把真实提交验收列为阻塞。
- N13-AC1：预览标题/正文/图片顺序与选定版本一致；账号不一致时不继续提交。
- N13-AC2：模拟提交超时/断线，显示结果待核查且无自动重复提交；手工标记与平台成功证据分别记录。

- 实施结果：新增 `client/src/lib/publishPreparation.ts`，从当前笔记生成不可变发布快照，检查标题、正文和媒体要求，使用账号/笔记/内容版本生成稳定 `snapshotKey`；发布尝试状态区分 prepared、submitted、confirmed、failed、unknown，并提供重复提交闸门。`Notes.tsx` 增加“发布检查”入口，只检查当前内容快照，不发起平台提交。
- 自动验证：`node --check scripts/check-publish-preparation.mjs`、`npm run check:publish-preparation`、`npm run build`、`git diff --check` 通过；未打开小红书发布页、未提交正式账号。
- AC 状态：N13-AC1 的内容快照和账号/版本边界已落地；真实图片顺序预览、浏览器账号核对待 D4/原生验收。N13-AC2 的状态模型和提交闸门已定义，未连接真实平台，因此不宣称超时/断线运行时证据。
- 未开放范围：平台填充、正式提交、发布成功核查和自动重试保持未启用；正式账号发布需要单独授权与可验证环境。

### N14 全链回归与文档收口

- 实施结果：按 N00–N13 的恢复入口复核前端检查脚本、CLI 探测、隔离 QA、SQLite 安全预检/备份恢复、Rust 数据库单测、前端构建和差异格式；同步更新任务索引与实施检查点，明确自动验证与原生产品验收的边界。
- 自动验证：`npm run qa:reset`、`npm run qa:prepare -- --json`、全部 `check:*` 脚本（含 `check:local-import`）、`npm run build`、`cargo fmt --manifest-path client/src-tauri/Cargo.toml -- --check`、`cargo check --manifest-path client/src-tauri/Cargo.toml`、`cargo test --manifest-path client/src-tauri/Cargo.toml db::tests -- --test-threads=1`（6 passed）、QA `db:preflight`/`db:import-plan`/`backup`/`restore-check` 和 `git diff --check` 均通过。备份恢复输出位于隔离 `/private/tmp`，正式数据未写入。
- AC 状态：自动可验证的账号隔离、版本保存、素材边界、能力提示、会话/提案持久化、剪藏校验、搜索偏好和发布快照均有对应脚本或 Rust 测试；真实 Tauri 窗口、关闭重开、A/B 竞态、大样本性能、Provider 取消、浏览器链路和平台提交仍保持未验收。
- 停止条件：不因构建或脚本通过而宣称去 Python 全链完成；D3/D4、正式导入与发布授权未决时，保留旧 Python CLI/MCP 和未迁移入口，不删除、不静默回退。

- 工作：逐项核对前述 AC，自动测试重点覆盖账号竞态、保存版本、缓存失效、文件/DB 补偿、AI 取消/冲突和发布幂等。按改动运行前端 build、Rust fmt/check/相关 tests；纯文档任务不启动服务或跑完整构建。
- N14-AC1：隔离资料完成选三图 → 创作 → AI 提案 → 采用 → 保存 → 切账号 → 重启恢复；有授权和环境时再覆盖浏览器链路，否则准确保留未验证项。
- N14-AC2：核心本地路径无 Python 依赖；所有残留旧服务能力有清单，不能仅因启动不依赖 Python 就宣布全量迁移结束。
- N14-AC3：逐项判断产品事实、技术事实、用户手册、发布说明、完成记录；只有用户验收后更新正式能力事实。原任务迁移表仍未完成的账号、人设、榜样等 CRUD 不因本文执行完就被忽略。

## 5. Noomd 参考地图

只读项目根：`/Users/wuxianhao/Workspace/Project/Personal/Note/noomd`。禁止在参考项目顺带修改。

| 主题 | 参考文件（相对 Noomd 根目录） | 借鉴边界 |
| --- | --- | --- |
| 保存与版本 | `src/components/editor/Editor.tsx`、`src-tauri/src/lib.rs` | 版本回执和冲突检查，不搬 Markdown 业务格式 |
| 异步与导航 | `src/App.tsx`、`src/workspace/openTabs.ts`、`navigationHistory.ts` | 请求隔离、恢复规则，不一次移植复杂页签 |
| 共享核心 | `crates/noomd-core/src/lib.rs`、`integrations/noomd-mcp/src/main.rs` | 核心与宿主分离；现有 path 引用布局不是必须复制的模板 |
| Provider/CLI | `src/components/settings/{ApiProviderSettings,LocalCliSettings}.tsx`、`src-tauri/src/{app_settings,agent_cli}.rs` | 逐适配器验证能力，不推定 CLI 等价 |
| AI 运行/提案 | `src-tauri/src/agent_runtime.rs`、`src/ai/{editProposal,sources,capturePolicy}.ts` | 取消、版本、来源；运行注册表不是断点续跑系统 |
| 收集 | `src/components/ai/InboxPanel.tsx`、`src-tauri/src/capture.rs` | 来源快照与业务转化分层 |
| 浏览器 | `integrations/noomd-browser-extension/`、`src-tauri/src/native_messaging.rs` | 当前页剪藏，不代表采集/发布已验证 |
| 视图 | `src/components/database/databaseViewConfig.ts` | 筛选/排序状态与业务数据分离，不移植通用数据库 |
| QA/边界 | `scripts/prepare-desktop-qa-workspace.mjs`、`check-rust-workspace-boundaries.mjs`、`package.json` | 隔离样本与针对性门禁，不照搬成本和全部依赖 |

## 6. 每项实施记录模板

在 `00-03-实施与验证记录.md` 追加正文，并同步顶部检查点和本文任务表：

```text
任务编号/名称：
时间/执行者/HEAD：
开始前状态、依赖与无关脏文件：
Read Set / Write Set：
已确认决定、待决问题：
实现内容与实际文件：
测试数据隔离方式：
检查命令及真实结果：
AC 编号 | 操作步骤 | 预期 | 实际结果 | 证据位置 | 责任人：
未验证/失败/阻塞：
五类文档落点：
下一项及恢复入口：
```

## 7. 可直接交给 Luna 的指令

> 请读取 AGENTS.md、文档导航、协作规则、CHG-20260916-001 确认卡、最新实施检查点及 `00-05-Noomd借鉴与Luna逐项执行清单.md`。按 N00–N14 顺序逐项实施，从第一项未完成且依赖满足的任务开始，每次明确承担的编号。先核对最新源码与脏状态，不重做已有能力；保留无关修改，不自动提交或推送，不修改 Noomd、移动端、官网、品牌源、MediaCrawler 或用户正式数据。先建立隔离测试入口，再完成本地保存/账号/素材闭环；后续接 AI、创作台、收集和发布准备。每项开始与结束更新任务表、实施记录和恢复检查点，列出真实 AC 结果。不要用构建通过代替产品验收；未获真实操作委托时提供用户验收步骤。D3/D4 只暂停依赖未决能力的动作，不重复询问已授权范围；不得擅自扩大浏览器权限或操作正式发布。完成当前项后报告下一项。
