# 爱吃红薯 AI 协作入口

## 阅读顺序

1. 阅读 [文档导航](docs/00-01-文档导航.md) 与 [协作规则](docs/产品研发文档/00-01-协作规则.md)。
2. 按用户任务在 [进行中事项索引](docs/产品研发文档/ING/00-01-进行中事项索引.md) 找到 CHG，读取确认卡、技术计划、恢复检查点。
3. 旧架构命令与工程注意事项见 [旧版工程运行说明](docs/开发参考/旧版工程运行说明.md)，仅作现状参考。

## 需求与授权

- “先对齐/先别改”：只读分析并按授权记录文档，不修改代码。
- “记 TODO”：只记想法，不承诺实现。
- 用户确认方案或明确交付执行后，在同一 CHG 内实施；最新用户授权优先于文档中的旧状态，及时同步，不重复确认已授权范围。
- 只读调查、文档整理、测试和环境检查不额外创建 CHG。
- 当前架构变更：CHG-20260916-001；阶段 A 盘点与 P0 启动/Rust SQLite 垂直切片已实施，现有业务页面逐页迁移仍未完成；以 CHG 实施记录为准。

## 必须保留的工程边界

- 保留无关未提交修改，不自动提交、推送或清理。
- 不提交 `.env`、用户数据库、素材、凭据或浏览器登录会话；不直接修改 `tools/MediaCrawler` 子模块。
- 旧 Python 浏览器路径仍使用 MC_PYTHON 与既有 Playwright 会话机制，不直接执行 Chromium 二进制；爬虫脚本从项目根目录运行。迁移方案需另行验证。
- 修改仍在使用的 Python 后端代码后，按工程说明重启对应服务；仅文档变更无需启动或重启。
- 实施、中断、交接前更新任务检查点；构建通过不等于产品验收通过。
